fixed:
  type: synapsespark
prompts:
  workspace:
    hint: "The name of the Synapse workspace"
  authentication:
    hint: "Azure Authentication method"
  schema:
    hint: "default schema / database"
  user:
    hint: "Who are you?"
  threads:
    hint: "1 or more"
    type: "int"
    default: 1
